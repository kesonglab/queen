# 更新日志

## v1.2.1 (2026-08-22)

### 新增
- 桌面通知：批量完成（含成功/失败统计）与单任务失败时，通过 macOS 通知中心弹出提示并伴随提示音。
- 吞吐调优：分片并发（`--concurrent-fragments`）由 4 提升至 16，新增 `--http-chunk-size 10M` 与
  `--buffer-size 16K`；默认并发下载数由 3 提升至 4。

### 修复
- 修正设置页语言切换的映射错位：语言行此前被绑定到错误索引（与下载目录行重叠），导致目录行静默改动
  语言、而语言行毫无反应。现映射已正确，并发上限放宽至 16。

### 技术栈
- Go + BubbleTea + Lip Gloss。
- 配置文件：`~/.config/videodl/config.json`。

## v1.2.0 (2026-08-22)

### 新增
- 并发多任务下载：并发数可在设置中调整（1–8，默认 3），多条链接并行下载、互不阻塞。
- 下载视图实时渲染全部进行中 / 已完成 / 失败任务；剩余任务以「等待中 × N」汇总。

### 修复
- 修复下载状态偶发「卡住不动」：进度静默期（如 ffmpeg 合并、封面嵌入）UI 不再停止重绘，spinner 持续动画。
- 修复进度 / 速度 / 剩余时间完全不显示：旧正则要求固定速度单位与 `MM:SS` 格式，导致 yt-dlp 的
  `Unknown B/s`、`ETA Unknown` 以及 `HH:MM:SS` 完成行全部匹配失败；现改为宽松解析，未知项留空而非丢弃整行。
- 修复中止逻辑：退出时正确终止所有运行的 yt-dlp 进程。
- 任务状态改为累积更新，避免进度行重建时丢失标题等信息。

### 技术栈
- Go + BubbleTea + Lip Gloss。
- 配置文件：`~/.config/videodl/config.json`。

## v1.1.0 (2026-08-19)

### 新增
- 界面更名为 Queen，全新视觉风格。
- 批量下载：进度条 + 已用时间 + 预估剩余时间。
- 应用内更新日志查看（本文件，TUI 菜单可读）。
- 中英双语界面（设置中切换）。
- 依赖自动检测，缺失时提示一键安装。
- 下载设置：播放列表 / 仅音频（mp3）/ 字幕 / 元数据嵌入 / Cookie 浏览器 / 重试次数 / 下载目录。
- 失败链接记录与一键重试。
- 单任务实时进度：百分比、大小、速度与剩余时间。

### 修复
- 进度数值更新不再产生水平布局抖动（定宽对齐）。

### 技术栈
- Go + BubbleTea + Lip Gloss（同 Mole）。
- 配置文件：`~/.config/videodl/config.json`。
- 失败记录：`~/Downloads/视频失败链接-failed_links.txt`。

## v1.0.0 (2026-08-19)
- 首个版本，由 bash 版视频下载脚本迁移而来。
